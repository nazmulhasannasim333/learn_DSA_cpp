#include <bits/stdc++.h>
using namespace std;

int main()
{
    int n;
    cin >> n;
    int a[n]; // ----> O(n) space complexity
    for (int i = 1; i <= n; i++)
    {
        cin >> a[i];
    }
    return 0;
}